一桁目は単語の機能を表す。

# 0（基数）
数を表す。この一文字だけの単語は「数」という概念を表す。

# 1（補数）
固有名詞に使う。

# 2（定数）
りんご、空、波など、その単語が指す種やカテゴリーが定められているもの。

# 3（変数）
代名詞や指示詞、疑問詞のような、指す対象が必ずいつも同じではないもの。

# 4（関数）
動詞などの機能を持つ。

# 5（係数）
形容詞や副詞などの機能や、単語の機能を変化させる役割をもつ。

# 6（複数）
集合を表すときに使う。

# 7（素数）
挨拶などの、それだけで文を完成させるもの。
